<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="VisiTour.Mobile.Views.HomePage"
             Title="{Binding Title}"
             x:Name="BrowseItemsPage">


        <ContentPage.Content>
        <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Image Source="background.jpg" AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All" Aspect="AspectFill"/>
            <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Margin="180">
            <Image Source="logo(2).png" WidthRequest="400"></Image>
                <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="600"/>
                        </Grid.ColumnDefinitions>
                    <Button x:Name="findFlights" Text="Find flights" Clicked="findFlights_Clicked" TextColor="White" BackgroundColor="OrangeRed" Margin="10"></Button>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="600"/>
                        </Grid.ColumnDefinitions>
                        <Button Text="Flight Classes" Clicked="class_Clicked" TextColor="White" BackgroundColor="OrangeRed" Margin="10"></Button>
                    </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="600"/>
                    </Grid.ColumnDefinitions>
                    <Button Text="Special offers" Clicked="offer_Clicked" TextColor="White" BackgroundColor="OrangeRed" Margin="10"></Button>
                </Grid>
                <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="600"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="About" Clicked="About_Clicked" Text="About us" TextColor="White" BackgroundColor="OrangeRed" Margin="10"></Button>
                    </Grid>
                </StackLayout>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>